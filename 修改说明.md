# psycoudbtv 修改说明

本文档说明 `psycoudbtv` 相对于上游 [pgvector-python](https://github.com/pgvector/pgvector-python)（基准版本 **0.4.2**）做了哪些修改、每处是怎么改的、为了实现什么，以及按什么顺序修改。

---

## 一、总体目标

把纯 Python 的 pgvector-python 改造成：

1. 包名为 **`psycoudbtv`**（而非 `pgvector`）；
2. 支持 **Python 3.8 ~ 3.13**（上游 0.4.2 要求 3.10+）；
3. 同时提供两条向量注册路径，分别对接两种驱动：
   - `psycoudbtv.psycopg2` → 原版 `psycopg2`
   - `psycoudbtv.psycounvdb` → 改名版 `psycounvdb`
4. 以"解压到 site-packages 即可用"的 zip 形式分发（不走 pip）。

因为 pgvector-python 是纯 Python、零运行时依赖、包内全部使用相对导入，所以改名的工作量很小，重点在驱动适配和打包方式。

---

## 二、逐项修改说明

### 修改 1：重命名包目录 `pgvector/` → `psycoudbtv/`

- **怎么改**：把整个顶层包目录从 `pgvector` 改名为 `psycoudbtv`。
- **为了实现什么**：让用户用 `import psycoudbtv` 导入。由于包内子模块全部是相对导入（如 `from .vector import Vector`），改名目录后**无需**修改任何内部 import 语句。

### 修改 2：`pyproject.toml` 的包名与 Python 版本

- **怎么改**：
  - `name = "pgvector"` → `name = "psycoudbtv"`
  - `requires-python = ">= 3.10"` → `">= 3.8"`
  - 配套加上 `[tool.setuptools.packages.find]` 的 `include = ["psycoudbtv*"]`
- **为了实现什么**：确定发布包的名字；把支持的最低 Python 版本下调到 3.8，以覆盖目标环境。

### 修改 3：`psycoudbtv/sqlalchemy/functions.py` 的命名空间标签

- **怎么改**：`package = 'pgvector'` → `package = 'psycoudbtv'`。
- **为了实现什么**：这是 SQLAlchemy 自定义函数（如 `avg`）的命名空间标签，并非 import 路径。改名只是为了与新包名保持一致，不影响 `from psycoudbtv.sqlalchemy import avg` 的用法。

### 修改 4：为驱动适配文件补 `from __future__ import annotations`

- **位置**：`psycoudbtv/psycopg2/{register,vector,halfvec,sparsevec}.py`（以及新建的 `psycounvdb/` 同名文件）。
- **怎么改**：在每个文件首行加入 `from __future__ import annotations`。
- **为了实现什么**：这些文件的函数签名里用了 `connection | cursor`、`str | None` 这类 `X | Y` 注解，而它在**函数定义时就会被求值**，Python 3.10 才支持。加上该 future 导入后，注解变成惰性字符串、不再求值，从而让这些子模块在 **Python 3.8/3.9 上也能正常 import**。这正是上游把 `requires-python` 标成 3.10+ 的根因。

### 修改 5：新增 `psycoudbtv/psycounvdb/` 子模块（对接 psycounvdb 驱动）

- **怎么改**：新建 `psycoudbtv/psycounvdb/` 包，包含 `__init__.py`、`register.py`、`vector.py`、`halfvec.py`、`sparsevec.py`，内容与 `psycopg2/` 版本相同，但驱动导入改为：
  - `import psycopg2` → `import psycounvdb as psycopg2`
  - `from psycopg2.extensions import ...` → `from psycounvdb.extensions import ...`
  - `__init__.py` 与 psycopg2 版一致：`from .register import register_vector`
- **为了实现什么**：提供一条 `from psycoudbtv.psycounvdb import register_vector` 的路径，其底层使用 `psycounvdb` 驱动（psycopg2 的改名版，`.extensions` API 完全一致）。用别名 `as psycopg2` 是为了让文件其余代码（如 `psycopg2.ProgrammingError`）无需再改。

### 修改 6：保持 `psycoudbtv/psycopg2/` 仍对接原版 psycopg2

- **怎么改**：`psycopg2/` 下的文件仍 `import psycopg2` / `from psycopg2.extensions import ...`（仅按修改 4 加了 future 导入）。
- **为了实现什么**：让两个子模块**各自只依赖同名的驱动**——用 `psycoudbtv.psycopg2` 不要求装 psycounvdb，用 `psycoudbtv.psycounvdb` 不要求装 psycopg2，两者可共存、互不影响。

### 修改 7：打包方式——生成"解压到 site-packages"的 zip（正斜杠路径）

- **怎么改**：用 `scripts/build_zip.py`（基于 Python `zipfile`）把 `psycoudbtv/` 目录打成 `psycoudbtv-<版本>-py3.8-3.13.zip`，**强制所有条目使用正斜杠 `/`**，并排除 `__pycache__`/`.pyc`。配套 GitHub Actions 工作流 `.github/workflows/build-psycoudbtv.yml` 自动构建、冒烟测试（解压并 import）、上传产物，打 `v*` tag 时附加到 Release。
- **为了实现什么**：产出一个解压即用的包。**关键点**：Windows 自带的 `Compress-Archive` 会用反斜杠 `\` 作分隔符，Linux 会把 `psycoudbtv\xxx.py` 当成单个文件名，导致解压不出目录、`import psycoudbtv` 失败。改用 Python `zipfile` 写正斜杠即可在 Linux/aarch64 正确解压。

---

## 三、修改顺序（可据此复现）

1. **重命名包目录** `pgvector/` → `psycoudbtv/`（修改 1）。
2. **改 `pyproject.toml`**：`name` 与 `requires-python`（修改 2）。
3. **改 `sqlalchemy/functions.py`** 的 `package` 标签（修改 3）。
4. **给 `psycopg2/` 适配文件加 future 导入**，解决 3.8/3.9 的 `X | Y` 注解问题（修改 4）。
5. **新建 `psycounvdb/` 子模块**，把驱动导入指向 `psycounvdb`（修改 5）。
6. **确认 `psycopg2/` 仍指向原版 psycopg2**，形成"两条路径各连各的驱动"（修改 6）。
7. **加入打包脚本与工作流**，产出正斜杠的 site-packages zip（修改 7）。

> 说明：实际开发中第 5、6 步经历过一次反复——最初让两条路径都指向 psycounvdb（用再导出的方式），后调整为"每个子模块对接各自同名驱动"。上表给出的是最终可复现的干净顺序。

---

## 四、验证情况

- 在 **aarch64 / Python 3.13.5 + UNVDB（PostgreSQL 24.11 分支，端口 5678，`vector` 0.8.0）** 上，解压到 site-packages 后：
  - `psycoudbtv.psycopg2` 与 `psycoudbtv.psycounvdb` 两条路径均成功 `register_vector`、写入 numpy 向量、并用 `<->` 完成最近邻检索，结果正确。
- 全部 `.py` 在 Python 3.8 上 `compileall` 通过，确认无 3.10+ 专有语法。

逐步连接测试见 `psycoudbtv-连接测试文档.md`。
